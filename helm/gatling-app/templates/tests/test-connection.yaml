apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gatling-app.fullname" . }}-test-connection"
  labels:
    {{- include "gatling-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gatling-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
